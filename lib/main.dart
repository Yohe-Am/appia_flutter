import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    title: 'Appia',
    theme: ThemeData(
      primarySwatch: Colors.pink,
    ),
    home: Center(
      child: const Text("Look in the test folder to access the demo"),
    ),
  ));
}
